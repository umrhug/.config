# .config
'configurations about neovim'

- touch .gitignore to write down the target files
- git init
- git add
- git commit -m 'comments'
- git branch -M main

- gh auth login
- gh repo create --public --source=. --remote=origin --push

